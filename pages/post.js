import LayoutBlog from '../components/layoutblog';
import DataPost from '../components/datapost';

const Post = () => {
    return (  
            <div>
                <LayoutBlog>
                        <DataPost/>
                </LayoutBlog>
            </div>

    );
}
 
export default Post;
